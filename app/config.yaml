# app/config.yaml
# ────────────────────────────────────────────────
# NEVER commit the real secrets version to git!
# Use environment-specific overrides or .gitignore'd copy

app:
  name: "swdl"
  debug: true
  host: "0.0.0.0"
  port: 8000

security:
  secret_key: "swdl219i39123iu9g1e12i912if9jg9wjehgh"
  session:
    max_age: 1209600       # 2 weeks in seconds
    same_site: "lax"

database:                    # example for later
  url: "sqlite:///dev.db"

features:
  enable_metrics: false
  enable_dark_mode_by_default: true

logging:
  level: "INFO"

